<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">
	<context:annotation-config></context:annotation-config>
	<context:property-placeholder location="/WEB-INF/props/jdbc.props" />
	<context:component-scan base-package="com.cglee079.portfolio.dao" />

	
	<bean id="dataSourceSpied" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>
	
	<!-- Query Log -->
	<bean id="dataSource" class="net.sf.log4jdbc.Log4jdbcProxyDataSource">
		<constructor-arg ref="dataSourceSpied" />
		<property name="logFormatter">
			<bean class="net.sf.log4jdbc.tools.Log4JdbcCustomFormatter">
				<property name="loggingType" value="MULTI_LINE" />
				<property name="sqlPrefix" value="SQL=>" />
			</bean>
		</property>
	</bean>

	<!-- Mybatis -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSourceSpied" />
		<property name="configLocation" value="classpath:mybatis/mybatis-config.xml" />
		<property name="mapperLocations" value="classpath:mybatis/mappers/**.xml" />
	</bean>

	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory"></constructor-arg>
	</bean>
	
	<!-- 
 	CREATE TABLE TB_PTFOLIO_ITEMS(
	ITM_SEQ int auto_increment,
	ITM_NAME varchar(200),
	ITM_SECT varchar(100),
	ITM_DESC varchar(1000),
	ITM_CONTENT varchar(5000),
	ITM_SNAPSHT varchar(500),
	ITM_DATE varchar(300),
	ITM_HASGIT boolean,
	ITM_GITURL	varchar(500),
	ITM_DEVELOPER varchar(300),
	ITM_HITS	int,
	constraint pk_ptfolio_items primary key(ITM_SEQ)
	)
	
	CREATE TABLE TB_PTFOLIO_ADMINS(
	ADMN_USERNAME varchar(200),
	ADMN_PASSWORD varchar(500),
	CONSTRAINT pk_ptfolio_admin PRIMARY KEY (ADMN_USERNAME)
	)

	INSERT TB_PTFOLIO_ADMINS VALUES("cglee079", "lcg050179!");
	
	CREATE TABLE TB_PTFOLIO_ADMNAUTHS(
	ADMN_USERNAME varchar(200),
	ADMN_ROLE varchar(200),
	CONSTRAINT pk_ptfolio_admnauths PRIMARY KEY(ADMN_USERNAME, ADMN_ROLE),
	CONSTRAINT fk_ptfolio_admnauths FOREIGN KEY (ADMN_USERNAME) REFERENCES TB_PTFOLIO_ADMINS(ADMN_USERNAME) ON DELETE CASCADE ON UPDATE CASCADE
	)
	
	INSERT TB_PTFOLIO_ADMNAUTHS VALUES("cglee079", "ROLE_ADMIN");
	
	
	CREATE TABLE TB_PTPOLIO_PHOTOS(
	PHOTO_SEQ int AUTO_INCREMENT,
	PHOTO_IMAGE varchar(500),
	PHOTO_SNAPSHT varchar(500),
	PHOTO_NAME varchar(200),
	PHOTO_DESC varchar(1000),
	PHOTO_LOC varchar(200),
	PHOTO_DATE varchar(100),
	PHOTO_DEVICE varchar(200),
	PHOTO_TAG varchar(200),
	PHOTO_LIKE int,
	constraint pk_ptfolio_photos primary key(PHOTO_SEQ)
	)
	-->
	
	
</beans>
