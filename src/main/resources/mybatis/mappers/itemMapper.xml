<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cglee079.portfolio.mapper.ItemMapper">
	<select id="list" resultType="com.cglee079.portfolio.model.Item">
		SELECT
		ITM_SEQ as "seq",
		ITM_NAME as "name",
		ITM_DESC as "desc",
		ITM_CONTENT as "content",
		ITM_SNAPSHT as "snapsht",
		ITM_WRDATE as "wrDate",
		ITM_DIRTURL as "dirtURL",
		ITM_ISDIRT as "dirt",
		ITM_HITS as "hit"
		FROM TB_PTFOLIO_ITEMS
	</select>
	
	<select id="get" resultType="com.cglee079.portfolio.model.Item" parameterType="Integer">
		SELECT
		ITM_SEQ as "seq",
		ITM_NAME as "name",
		ITM_DESC as "desc",
		ITM_CONTENT as "content",
		ITM_SNAPSHT as "snapsht",
		ITM_WRDATE as "wrDate",
		ITM_DIRTURL as "dirtURL",
		ITM_ISDIRT as "dirt",
		ITM_HITS as "hit"
		FROM TB_PTFOLIO_ITEMS
		WHERE ITM_SEQ = #{seq}
	</select>
	
	<delete id="delete">
		DELETE FROM TB_PTFOLIO_ITEMS
		WHERE ITM_SEQ = #{seq}
	</delete>
	
	<insert id="insert">
		INSERT TB_PTFOLIO_ITEMS (ITM_NAME, ITM_DESC, ITM_CONTENT, ITM_SNAPSHT, ITM_WRDATE, ITM_DIRTURL, ITM_ISDIRT, ITM_HITS)
		VALUES (#{name}, #{desc}, #{content}, #{snapsht}, #{wrDate}, #{dirtURL}, #{dirt}, #{hits})
	</insert>
	
	
</mapper>